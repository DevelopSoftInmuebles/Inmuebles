{# % extends '::base.html.twig' %}

{% block body -%}
	{{ form(formIngreso) }}
{% endblock %#}

{% extends '::base.html.twig' %}

{% block body -%}
	
    {{ form(form) }}
    <a href="{{path('inmuebles_homepage')}}">Cancelar</a>
    

{% endblock %}


{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar Inmueble</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a class="btn" href="{{ path('inmueble') }}">
            Volver al a lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
